<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:SaveEditor.Ui.Shell;assembly=SaveEditor.Ui"
        x:Class="SuikodenHdSaveEditor.App.MainWindow"
        Title="Suikoden I &amp; II HD Remaster Save Editor"
        Width="1280" Height="800"
        MinWidth="980" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}">

    <shell:EditorShell x:Name="Shell">

        <shell:EditorShell.Branding>
            <StackPanel>
                <TextBlock Text="S I · II"
                           Foreground="{DynamicResource Foreground}"
                           FontSize="22" FontWeight="Bold" />
                <TextBlock Text="HD SAVE EDITOR"
                           Foreground="{DynamicResource SubtleForeground}"
                           FontSize="10" />
            </StackPanel>
        </shell:EditorShell.Branding>

        <shell:EditorShell.HeaderActions>
            <Button x:Name="RestoreButton"
                    Content="Restore from backup…"
                    AutomationProperties.Name="Restore from backup" />
        </shell:EditorShell.HeaderActions>

        <!-- Folder slots stay app-owned: the framework can open a folder, but picking one of
             the Data0-Data16 slots it finds is this game's convention, not a general one. -->
        <shell:EditorShell.SidebarExtension>
            <StackPanel Spacing="6">
                <TextBlock Text="Folder slots"
                           Foreground="{DynamicResource SubtleForeground}"
                           FontSize="11" />
                <ComboBox x:Name="SlotPicker"
                          HorizontalAlignment="Stretch"
                          AutomationProperties.Name="Detected save slots" />
                <Button x:Name="OpenSlotButton"
                        Content="Open selected slot"
                        HorizontalAlignment="Stretch"
                        AutomationProperties.Name="Open selected slot" />
            </StackPanel>
        </shell:EditorShell.SidebarExtension>

    </shell:EditorShell>

</Window>
